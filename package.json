{
  "name": "breakout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Klasik blok kırma (Breakout) oyunu — vanilla Canvas 2D istemci + minimal Express statik sunucu.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "4.21.2"
  },
  "devDependencies": {}
}
